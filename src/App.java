public class App {
    public static void main(String[] args){
        System.out.println("Hello, World!");
        
        // creating DataType class object and accessing its class attribute/field x. 
        //Note: class name should be same as file name
        // Note: both files should be in same folder to access them from different files.
        DataType obj = new DataType();
        System.out.println(obj.x);
    }
}
